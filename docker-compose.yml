version: '3.3'
services:
    api:
        build: .
        image: crc.us:latest
        ports:
            - 5060:5000
        volumes:
            - type: bind
              source: .
              target: /app/